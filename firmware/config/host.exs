import Config
# Enter here configuration for running on host
# For example, if you do export MIX_TARGET=host, 
# configuration will be read from this file
#
config :gas_sensor, :env, :host # used  in Application.get_env(:gas_sensor, :env, :host) at 
                                # gas_sensor/lib/gas_sensor/timstamp.ex/ntp_synced?
